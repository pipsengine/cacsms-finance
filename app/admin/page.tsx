'use client';import {useEffect,useState} from 'react';import AdminShell from '@/components/admin/AdminShell';import AdminHeader from '@/components/admin/AdminHeader';
const money=(n:number)=>new Intl.NumberFormat('en-NG',{style:'currency',currency:'NGN',maximumFractionDigits:0}).format(n);
export default function Admin(){const[d,setD]=useState<any>(null);useEffect(()=>{fetch('/api/admin/overview').then(r=>r.json()).then(setD)},[]);return <AdminShell><AdminHeader eyebrow="PLATFORM CONTROL" title="Administration overview" description="Users, subscriptions, revenue and platform activity in one place."/><section className="adminKpis"><div><span>Total users</span><b>{d?.users??'—'}</b><small>{d?.activeUsers??0} active</small></div><div><span>Paid subscribers</span><b>{d?.paidSubscribers??'—'}</b><small>{d?.trials??0} trials</small></div><div><span>Estimated MRR</span><b>{d?money(d.mrr):'—'}</b><small>Monthly recurring revenue</small></div><div><span>Payments collected</span><b>{d?money(d.payments):'—'}</b><small>Recorded successful payments</small></div></section><section className="adminCard"><h3>Plan distribution</h3><div className="planBars">{(d?.plans||[]).map((x:any)=><div key={x.name}><span>{x.name}</span><div><i style={{width:`${Math.max(4,(x.count/Math.max(1,d.users))*100)}%`}}/></div><b>{x.count}</b></div>)}</div></section></AdminShell>}
