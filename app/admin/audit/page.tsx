'use client';import {useEffect,useState} from 'react';import AdminShell from '@/components/admin/AdminShell';import AdminHeader from '@/components/admin/AdminHeader';
export default function Audit(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/admin/audit').then(r=>r.json()).then(setRows)},[]);return <AdminShell><AdminHeader eyebrow="GOVERNANCE" title="Audit log" description="Trace security, administration, subscription and payment actions."/><div className="adminTable"><table><thead><tr><th>Time</th><th>Action</th><th>Entity</th><th>Actor</th><th>Details</th></tr></thead><tbody>{rows.map(a=><tr key={a.id}><td>{new Date(a.createdAt).toLocaleString()}</td><td><b>{a.action}</b></td><td>{a.entity}<small>{a.entityId}</small></td><td>{a.actorUserId}</td><td className="auditDetail">{a.details||'—'}</td></tr>)}</tbody></table></div></AdminShell>}
